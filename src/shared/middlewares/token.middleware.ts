import { Request, Response, NextFunction } from 'express'; import crypto from 'crypto'; /** * Token 生成记录 (内存存储，生产环境应使用 Redis) */ const activeTokens = new Map<string, { userId: string; expiresAt: number; downloadCount: number; }>(); /** * Token 有效期 (默认 1 小时) */ const TOKEN_EXPIRY = 60 * 60 * 1000; /** * 最大下载次数 */ const MAX_DOWNLOAD_COUNT = 5; /** * 生成一次性下载 Token * @param userId 用户 ID * @returns Token 字符串 */ export function generateDownloadToken(userId: string = 'anonymous'): string { const token = crypto.randomBytes(32).toString('hex'); const expiresAt = Date.now() + TOKEN_EXPIRY; activeTokens.set(token, { userId, expiresAt, downloadCount: 0, }); // 清理过期 token 的定时器 setTimeout(() => { activeTokens.delete(token); }, TOKEN_EXPIRY); return token; } /** * 验证下载 Token 的中间件 */ export function verifyDownloadToken(req: Request, res: Response, next: NextFunction): void { const token = req.query.token as string || req.headers['x-download-token'] as string; if (!token) { return res.status(401).json({ success: false, message: '缺少下载凭证 (token)' }); } const tokenData = activeTokens.get(token); if (!tokenData) { return res.status(403).json({ success: false, message: '无效的下载凭证' }); } if (Date.now() > tokenData.expiresAt) { activeTokens.delete(token); return res.status(403).json({ success: false, message: '下载凭证已过期' }); } if (tokenData.downloadCount >= MAX_DOWNLOAD_COUNT) { activeTokens.delete(token); return res.status(403).json({ success: false, message: '下载次数已达上限' }); } // 增加下载次数 tokenData.downloadCount += 1; activeTokens.set(token, tokenData); // 将 token 信息附加到请求对象 (req as any).tokenData = tokenData; next(); } /** * 验证 Token 并包装下载响应 */ export function verifyAndDownload(req: Request, res: Response, filePath: string, fileName: string): void { const token = req.query.token as string; if (!token) { return res.status(401).json({ success: false, message: '缺少下载凭证' }); } const tokenData = activeTokens.get(token); if (!tokenData) { return res.status(403).json({ success: false, message: '无效的下载凭证' }); } if (Date.now() > tokenData.expiresAt) { activeTokens.delete(token); return res.status(403).json({ success: false, message: '下载凭证已过期' }); } if (tokenData.downloadCount >= MAX_DOWNLOAD_COUNT) { activeTokens.delete(token); return res.status(403).json({ success: false, message: '下载次数已达上限' }); } tokenData.downloadCount += 1; activeTokens.set(token, tokenData); res.download(filePath, fileName, (err) => { // 清理逻辑可根据需要添加 }); } /** * 清理所有过期 token */ export function cleanupExpiredTokens(): void { const now = Date.now(); for (const [token, data] of activeTokens.entries()) { if (now > data.expiresAt) { activeTokens.delete(token); } } }
